#lombok.config文件需要放在src/main/java文件夹下的目录中（也可以放在实体同级目录下），
#放在src/main/resources目录下，不会生效

config.stopBubbling=true
# 集中解决使用lombok时，子类equals和hashcode方法忽略父类属性的问题
lombok.equalsAndHashCode.callSuper=CALL
# 集中解决使用lombok时，子类tostring方法忽略父类属性的问题
lombok.tostring.callsuper=CALL